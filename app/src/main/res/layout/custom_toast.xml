<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/custom_toast_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_margin="@dimen/custom_transcript_toast_margin"
            android:padding="@dimen/custom_transcript_padding"
            android:background="@drawable/rectangle_border_shape">

            <ImageView
                android:src="@drawable/footstep_by_netalloy_300x300"
                android:layout_width="@dimen/transcripts_logo_width"
                android:layout_height="@dimen/transcripts_logo_height"
                android:layout_alignParentTop="true"
                android:layout_alignParentEnd="true"
                android:padding="@dimen/transcripts_logo_padding"
                android:scaleType="centerCrop"
                android:contentDescription="@string/logo"/>

            <LinearLayout
                style="@style/normal"
                android:orientation="vertical"  >

                <TextView
                    android:id="@+id/score"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginBottom="@dimen/little_margin_top_bottom"
                    android:textSize="@dimen/second_level_font_size"
                    android:fontFamily="sans-serif-condensed"
                    android:textColor="@android:color/background_dark"/>

                <TextView
                    android:id="@+id/resultMessageTip"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginBottom="@dimen/little_margin_top_bottom"
                    android:text="@string/error_reason_below"
                    android:textSize="@dimen/second_level_font_size"
                    android:visibility="invisible"/>

                <TextView
                    android:id="@+id/resultMessage"
                    style="@style/normal"
                    android:textSize="@dimen/third_level_font_size"/>
            </LinearLayout>

        </RelativeLayout>



</ScrollView>